#include<stdio.h>
int main()
{ 
    int a[5] = {5,4,3,2,1};
    int n =5;
     for(int i = 0; i<n ; i++)
     {
        printf("%d",a[i]);
     }
     // Sorting
      for(int i = 0 ; i< n-1 ; i++)
      {
        for(int j = 0)
      }
    return 0;
}